import { useSelector } from "react-redux";
import { ClientData } from "../models/ClientData";
import { StateType } from "../redux/store";

//export const currentUser = useSelector<StateType, ClientData>(state => state.clientData)